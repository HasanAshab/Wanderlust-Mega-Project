pipeline {
    agent any

    options {
        buildDiscarder(logRotator(numToKeepStr: '5', artifactNumToKeepStr: '5'))
        disableConcurrentBuilds()
        durabilityHint('PERFORMANCE_OPTIMIZED')
        preserveStashes(buildCount: 5)
    }

    parameters {
        string(name: 'IMAGE_TAG', defaultValue: '', description: 'Docker image tag for latest push')
    }

    stages {
        stage('Validate Params') {
            steps {
                script {
                    if (!params.IMAGE_TAG?.trim()) {
                        error "Parameter IMAGE_TAG is required but not provided!"
                    }
                }
            }
        }

        stage('Install Dependencies') {
            steps {
                sh 'npm ci'
            }
        }

        stage('CI') {
            parallel {
                stage('Run Lint') {
                    steps {
                        sh 'npm run lint'
                    }
                }

                stage('Run Tests') {
                    steps {
                        sh 'npm test'
                    }
                }
            }
        }
    }

    post {
        always {
            cleanWs()
        }
        success { echo '✅ Pipeline succeeded!' }
        failure { echo '❌ Pipeline failed!' }
    }
}
